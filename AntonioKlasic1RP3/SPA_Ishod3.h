#pragma once
#include "SPA_Heroj.h"

namespace SPA {
	
	void Funkcionalnost_PretragaPoImenu(SPA::SuperHeroHolder& Heros);
	void Funkcionalnost_PrikazPoBrzini(SPA::SuperHeroHolder& Heroes);
	void Funkcionalnost_KopiranjeUNoviVektor(SPA::SuperHeroHolder& Heores);
	void Funkcionalnost_PrikazPoStrani(SPA::SuperHeroHolder& Heroes);

}